const { linearSearch } = require('./searching');
const { calculateSum } = require('./sum');
const { calculateMultiply } = require('./multiply');
module.exports = { linearSearch, calculateSum, calculateMultiply };
5;
